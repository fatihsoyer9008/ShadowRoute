# deploy/.env — sunucuya özel değerler (bu dosyayı kopyalayıp doldur; .env git'e girmez)
SERVER_HOST=1.2.3.4
API_SUBDOMAIN=golgerota.example.com
APP_PORT=8010

# Opsiyonel: openweathermap.org'dan ücretsiz API key. Boşsa hava durumu
# özelliği sessizce kapalı kalır (analiz etkilenmez).
OPENWEATHER_API_KEY=
